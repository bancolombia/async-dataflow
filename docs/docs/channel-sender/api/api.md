# API Documentation

In the next section we will describe the API of ADF Channel Sender.

You also can find a Swagger definition of the API in the file [`/channel-sender/swagger.yaml`](https://github.com/bancolombia/async-dataflow/blob/master/channel-sender/swagger.yaml). You can run it in `https://editor.swagger.io/` to get a preview of the documentation.
